	set(CURRENT_DIR tests/widgets/Composite)
	set(src_widgets
		${src_widgets}
		${CURRENT_DIR}/TComposites.cpp
		${CURRENT_DIR}/TCompositeDemo.cpp
		${CURRENT_DIR}/TScrolledComposite.cpp
		PARENT_SCOPE
	)
	